import Operator from "./Operator";

export default function Operators() {
    return (
        <div className="operators">
            <Operator>+</Operator>
            <Operator>-</Operator>
            <Operator>*</Operator>
            <Operator>/</Operator>
            <Operator>=</Operator>
        </div>

    )
}